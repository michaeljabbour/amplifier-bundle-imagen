bundle:
  name: imagegen-behavior
  version: 2.0.0
  description: Full adaptive image studio — generation, editing, specialist review, skill discovery, policy preflight, approval gates, and artifact delivery

includes:
  - bundle: imagen:behaviors/image-generation
  - bundle: imagen:behaviors/image-editing

# tool-imagen is contributed by each à-la-carte behavior so either behavior is
# complete when included alone. Skill discovery is contributed by generation.

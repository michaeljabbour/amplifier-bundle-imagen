bundle:
  name: imagen-local-dev
  version: 2.0.0
  description: Dev variant using the local bundle checkout — for testing bundle changes before push. Loads the native tool-imagen module from the same checkout.

includes:
  - bundle: git+https://github.com/microsoft/amplifier-foundation@v2.1.2
  - bundle: imagen:behaviors/imagegen
  - bundle: foundation:providers/anthropic-opus
